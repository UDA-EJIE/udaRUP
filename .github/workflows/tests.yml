name: 🧪 Front-end tests

# ⚡ Cuándo se ejecuta este workflow
on:
  push:
    # Se ejecuta al hacer push a estas ramas
    branches: ["master", "develop*", "feat-*", "hotfix-*", "fix-*"]
  pull_request:
    # Se ejecuta al crear/actualizar PRs hacia estas ramas
    branches: ["master", "develop*", "feat-*", "hotfix-*", "fix-*"]

# 🚫 Cancelar workflows anteriores si hay nuevos commits (ahorra recursos)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (Node.js ${{ matrix.node-version }})
    runs-on: ubuntu-latest

    # 🔒 Permisos mínimos necesarios para seguridad
    permissions:
      contents: read # Leer el código del repositorio
      checks: write # Escribir resultados de tests
      pull-requests: write # Comentar en PRs (si se necesita)

    # 🎯 Configuración de versiones a probar
    strategy:
      fail-fast: false # No cancelar si falla, continuar con otras tareas
      matrix:
        node-version: [22.x] # Solo Node.js 22 (versión que usamos)

    steps:
      # 📥 Descargar el código del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Descargar historial completo (útil para análisis)
          fetch-depth: 0

      # 🟢 Configurar Node.js con caché automático de npm
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm" # Caché automático de dependencias
          cache-dependency-path: x21a_v6/udaRUP/package-lock.json # Path específico para cache
          registry-url: "https://registry.npmjs.org"

      # ℹ️ Mostrar versiones instaladas (útil para depuración)
      - name: Verify Node.js and npm versions
        run: |
          echo "Node.js version: $(node --version)"
          echo "npm version: $(npm --version)"

      # 📦 Instalar dependencias del proyecto
      - name: Install dependencies
        working-directory: x21a_v6/udaRUP
        run: |
          # Si existe package-lock.json, usar npm ci (más rápido y determinista)
          # Si no, usar npm install tradicional
          if [ -f package-lock.json ]; then
            npm ci --no-progress --prefer-offline
          else
            npm install --no-progress
          fi

      # 🏗️ Compilar proyecto (genera carpeta dist/)
      - name: Build project
        working-directory: x21a_v6/udaRUP
        run: npm run build

      # 🧪 Ejecutar pruebas unitarias con Jasmine/Karma
      - name: Run unit tests
        working-directory: x21a_v6/udaRUP
        run: npm run test:karma:ci
        env:
          # Variable de entorno para CI
          NODE_ENV: test

      # 💾 Guardar reportes de cobertura como artefactos
      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        if: always() # Subir siempre, incluso si fallan las pruebas
        with:
          name: coverage-reports-node-${{ matrix.node-version }}
          path: x21a_v6/udaRUP/coverage/ # Solo reportes de coverage
          retention-days: 30 # Guardar por 30 días

  # 📋 Resumen de la tarea: verificar que todas las pruebas pasaron
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: test # Esperar a que termine la tarea 'test'
    if: always() # Ejecutar siempre, incluso si 'test' falla

    steps:
      # ✅ Verificar resultado final
      - name: Check test results
        run: |
          if [[ "${{ needs.test.result }}" == "success" ]]; then
            echo "✅ All tests passed successfully!"
            exit 0
          else
            echo "❌ Some tests failed. Check the logs above."
            exit 1
          fi
